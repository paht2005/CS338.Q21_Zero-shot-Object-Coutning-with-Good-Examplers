---
phase: 01-server-setup-baseline-reproduction
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - scripts/run_evaluation_val.sh
  - experiments/server-val-baseline/RESULTS.md
autonomous: false

requirements:
  - SRV-04

must_haves:
  truths:
    - "val-split MAE and RMSE are recorded for all 4 pipeline configurations"
    - "Rich Prompts reduce MAE on val split for GroundingDINO (baseline vs. DINO+RP)"
    - "YOLO-World + Rich Prompt achieves MAE within 1 point of GroundingDINO + Rich Prompt on val split"
    - "Results are committed to experiments/server-val-baseline/RESULTS.md with MAE/RMSE for all 4 configs"
  artifacts:
    - path: "scripts/run_evaluation_val.sh"
      provides: "Evaluation script for val split across all 4 configs (NAS data paths at /mnt/mmlab2024nas/counting)"
      exports: []
    - path: "experiments/server-val-baseline/RESULTS.md"
      provides: "MAE/RMSE table for all 4 configs on val split"
      contains: "MAE"
  key_links:
    - from: "scripts/run_evaluation_val.sh"
      to: "code/source-code/FSC_test.py"
      via: "--split val flag"
      pattern: "--split val"
    - from: "code/source-code/FSC_test.py"
      to: "code/source-code/data/FSC147"
      via: "--data_split_file argument"
      pattern: "data_split_file"
---

<objective>
Run val-split evaluation for all 4 pipeline configurations on counting@192.168.6.200 and record the baseline numbers.

Purpose: Establish the val-split MAE/RMSE baselines before adding new detectors (Phase 2-4). These numbers confirm the server env correctly reproduces the pipeline and provide a consistent comparison point for new detectors. Data served from NAS at /mnt/mmlab2024nas/counting.

Output: scripts/run_evaluation_val.sh (4 configs, --split val, DATA_BASE=/mnt/mmlab2024nas/counting), and experiments/server-val-baseline/RESULTS.md with MAE/RMSE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@scripts/run_evaluation.sh
@code/source-code/FSC_test.py

<interfaces>
FSC_test.py key CLI arguments:
  --data_split_file   path to Train_Test_Val_FSC_147.json
  --im_dir            path to images_384_VarV2/
  --gt_dir            path to gt_density_map_adaptive_384_VarV2/
  --anno_file         path to positive exemplar annotation JSON
  --output_dir        where to save outputs
  --resume            path to NSM checkpoint (.pth)
  --split             "train" | "val" | "test" (default: "test")
  --external          flag to use pre-computed exemplars

NAS data base: /mnt/mmlab2024nas/counting (DATA_BASE env var)
FSC147:        ${DATA_BASE}/FSC147/
Checkpoints:   ${DATA_BASE}/*.pth

4 configurations:
  1. VA-Count baseline:          checkpoint_FSC.pth + annotation_FSC147_384.json
  2. VA-Count + Rich Prompt:     checkpoint__finetuning_dino_prompt.pth + annotation_FSC147_pos.json
  3. VA-Count + YOLO (no RP):    checkpoint__finetuning_yolo_noprompt.pth + annotation_FSC147_pos_yolo_noprompt.json
  4. VA-Count + YOLO + Rich RP:  checkpoint__finetuning_yolo.pth + annotation_FSC147_pos_yolo_prompt.json
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify run_evaluation_val.sh for local data paths</name>
  <files>scripts/run_evaluation_val.sh</files>
  <action>
Verify scripts/run_evaluation_val.sh has been updated to use NAS data paths (counting@192.168.6.200).

It must:
1. Use DATA_BASE="${DATA_BASE:-/mnt/mmlab2024nas/counting}" (overridable env var, default NAS path)
2. Run all 4 configs with --split val using the local FSC147 path
3. Skip configs where annotation or checkpoint is missing (print SKIPPED, don't fail hard)
4. Log each config to experiments/server-val-baseline/{config_name}.log
5. Print a RESULTS SUMMARY at the end with grep output

Config names: baseline, dino_rich, yolo_norich, yolo_rich

If the script still references old local paths (code/source-code/data) as hardcoded defaults, replace with /mnt/mmlab2024nas/counting.
  </action>
  <verify>
    <automated>bash -n scripts/run_evaluation_val.sh &amp;&amp; grep -q "mmlab2024nas" scripts/run_evaluation_val.sh &amp;&amp; grep -q "\-\-split val" scripts/run_evaluation_val.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <done>run_evaluation_val.sh passes bash -n; DATA_BASE defaults to /mnt/mmlab2024nas/counting; --split val in every python call; 4 configs with skip logic</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Run evaluation on server and collect MAE/RMSE</name>
  <files>N/A — server-side execution</files>
  <action>SSH into counting@192.168.6.200 (pass: 1), activate conda env (conda activate cs338), run: bash scripts/run_evaluation_val.sh, then grep logs for MAE/RMSE and paste results.</action>
  <verify>User reports MAE/RMSE for at least 2 configs (or SKIPPED with reason for others)</verify>
  <done>MAE and RMSE numbers collected for all completed configs; ready for RESULTS.md</done>
  <what-built>run_evaluation_val.sh with 4 configs, local data paths, skip logic for missing files</what-built>
  <how-to-verify>
Step 1 — Push on local machine:
  git add scripts/run_evaluation_val.sh &amp;&amp; git push

Step 2 — On server:
  ssh counting@192.168.6.200
  cd ~/cs338-counting
  conda activate cs338
  git pull

Step 3 — Run evaluation (30-90 min per config depending on GPU):
  bash scripts/run_evaluation_val.sh

Step 4 — Extract MAE/RMSE:
  grep -rE "MAE|RMSE|mae|rmse" experiments/server-val-baseline/*.log

Report back:
- MAE and RMSE for each config that completed
- Which configs were SKIPPED (missing annotation/checkpoint)
- Any error messages
  </how-to-verify>
  <resume-signal>
Paste the grep output showing MAE/RMSE per config, e.g.:
  baseline: MAE=XX.XX RMSE=YYY.YY
  dino_rich: MAE=XX.XX RMSE=YYY.YY
  yolo_norich: MAE=XX.XX RMSE=YYY.YY (or SKIPPED)
  yolo_rich: MAE=XX.XX RMSE=YYY.YY
  </resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record results to RESULTS.md and commit</name>
  <files>experiments/server-val-baseline/RESULTS.md</files>
  <action>
After the user reports back MAE/RMSE numbers from Task 2, create experiments/server-val-baseline/RESULTS.md.

Fill in actual numbers reported by the user. Use 0.00 or SKIPPED for any config that did not complete.

Template:
---
# Val-Split Baseline Results — FSC-147

**Date:** 2026-05-13
**Server:** counting@192.168.6.200 (Conda env cs338, NAS data at /mnt/mmlab2024nas/counting)
**Split:** val (1,108 images)

## Results

| Config | MAE down | RMSE down | Notes |
|--------|----------|-----------|-------|
| VA-Count baseline (GDino, raw) | X.XX | XXX.XX | |
| VA-Count + Rich Prompt (GDino) | X.XX | XXX.XX | |
| VA-Count + YOLO-World (no RP)  | X.XX | XXX.XX | |
| VA-Count + YOLO-World + Rich Prompt | X.XX | XXX.XX | |

## Comparison vs. Test Split (paper)

| Config | Val MAE | Test MAE | Delta |
|--------|---------|----------|-------|
| VA-Count baseline | X.XX | 17.99 | +X.XX |
| VA-Count + Rich Prompt | X.XX | 17.80 | +X.XX |
| VA-Count + YOLO-World | X.XX | 19.03 | +X.XX |
| VA-Count + YOLO-World + RP | X.XX | 17.91 | +X.XX |

## Rich Prompt Delta MAE (val split)

| Detector | Raw MAE | +Rich Prompt MAE | ΔMAE |
|----------|---------|------------------|------|
| GroundingDINO | X.XX | X.XX | -X.XX |
| YOLO-World | X.XX | X.XX | -X.XX |

## Notes

- Configs SKIPPED (missing files): [list or "None"]
- Annotation files used and actual paths: [list]
- Server: counting@192.168.6.200 (Conda env cs338), data at /mnt/mmlab2024nas/counting
---

Then commit:
  git add experiments/server-val-baseline/RESULTS.md scripts/run_evaluation_val.sh
  git commit -m "feat(01-02): add val-split baseline results from sandbox"
  </action>
  <verify>
    <automated>test -f experiments/server-val-baseline/RESULTS.md &amp;&amp; grep -q "MAE" experiments/server-val-baseline/RESULTS.md &amp;&amp; echo "RESULTS.md exists with MAE"</automated>
  </verify>
  <done>RESULTS.md exists with actual MAE/RMSE filled in; Rich Prompt delta section shows direction; file committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server process to local data | Reads FSC-147 files from local disk (no network) |
| server process to Gemini API | Only for Rich Prompt generation (pre-computed, not at eval time) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | Log files in experiments/ | accept | Internal research logs; no integrity requirement |
| T-02-02 | Information Disclosure | RESULTS.md committed to git | accept | No PII or secrets; research results only |
</threat_model>

<verification>
- bash -n scripts/run_evaluation_val.sh passes
- No NAS path references in script
- RESULTS.md exists with actual numbers from server run
- Rich Prompt delta is negative (improvement) for at least one detector
</verification>

<success_criteria>
- run_evaluation_val.sh committed and syntactically valid for local paths
- User provides MAE/RMSE for at least 2 of 4 configs
- RESULTS.md committed with real numbers
- Delta MAE confirms Rich Prompts reduce error on val split
</success_criteria>

<output>
After completion, create .planning/phases/01-server-setup-baseline-reproduction/01-02-SUMMARY.md
</output>
