---
phase: 01-server-setup-baseline-reproduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/server_setup.sh
  - scripts/verify_env.sh
autonomous: false

requirements:
  - SRV-01
  - SRV-02
  - SRV-03

must_haves:
  truths:
    - "Server GPU is accessible (nvidia-smi shows Tesla T4, CUDA available in PyTorch)"
    - "Python .venv is created at ~/cs338-counting/.venv with all deps installed"
    - "FSC-147 images and annotation JSONs are present under code/source-code/data/FSC147/"
    - "All 4 NSM checkpoints are present under code/source-code/data/"
    - "GEMINI_API_KEY is configured in code/source-code/.env on server"
  artifacts:
    - path: "scripts/server_setup.sh"
      provides: "Reproducible venv-based environment setup script for sandbox.netbird.cloud"
      exports: []
    - path: "scripts/verify_env.sh"
      provides: "Quick smoke-test for all imports and local data paths"
      exports: []
  key_links:
    - from: "scripts/server_setup.sh"
      to: "code/source-code/GroundingDINO"
      via: "pip install -e ."
      pattern: "pip install -e"
    - from: "scripts/verify_env.sh"
      to: "code/source-code/data/FSC147"
      via: "path existence check"
      pattern: "test -e"
---

<objective>
Prepare server `phatcnguyen@sandbox.netbird.cloud` for reproducing the 4-config val-split evaluation.

Purpose: Establish a reproducible Python venv environment on the team sandbox server so all downstream experiment runs (Phases 2-4) execute consistently. Data lives locally under code/source-code/data/ (no NAS mount on this server).

Server specs: Tesla T4 (15GB VRAM), CUDA 13.2, Ubuntu 24.04, Python 3.12, 94GB free disk.
SSH: ssh sandbox.netbird.cloud (key-based via ~/.ssh/id_ed20260404 — no password needed).

Output: scripts/server_setup.sh and scripts/verify_env.sh updated for sandbox; user runs them on server and confirms environment works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@scripts/setup_env.sh
@code/source-code/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify server_setup.sh for venv on sandbox.netbird.cloud</name>
  <files>scripts/server_setup.sh</files>
  <action>
Verify scripts/server_setup.sh has been updated for sandbox.netbird.cloud (venv, local paths, no conda/NAS).

It must contain all 8 steps:
1. Clone/pull repo from https://github.com/paht2005/CS338.Q21_Zero-shot-Object-Coutning-with-Good-Examplers.git to ~/cs338-counting
2. Create Python 3.12 venv at ~/cs338-counting/.venv
3. Install PyTorch with --index-url https://download.pytorch.org/whl/cu121 (compatible with CUDA 13.2 hardware)
4. Set CUDA_HOME=/usr/local/cuda-13.2 and install GroundingDINO editable
5. Install requirements.txt
6. Install CLIP via git+https
7. Check local data paths under code/source-code/data/FSC147/
8. List checkpoint files under code/source-code/data/

If any step is missing or still references counting@192.168.6.200, NAS paths, or conda, fix it immediately.
  </action>
  <verify>
    <automated>bash -n scripts/server_setup.sh &amp;&amp; ! grep -q "mmlab2024nas\|conda create\|192.168.6.200" scripts/server_setup.sh &amp;&amp; grep -q "venv\|cu121" scripts/server_setup.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <done>scripts/server_setup.sh passes bash -n; references venv + cu121; no conda/NAS/old server references</done>
</task>

<task type="auto">
  <name>Task 2: Verify verify_env.sh for local data paths</name>
  <files>scripts/verify_env.sh</files>
  <action>
Verify scripts/verify_env.sh has been updated for local data paths (no NAS).

It must contain all 5 check categories:
1. Import checks: torch, clip, ultralytics, groundingdino, google.generativeai, timm — each as separate python -c call with PASS/FAIL output
2. GPU check: assert torch.cuda.is_available(), print device name
3. Local data path checks: code/source-code/data/FSC147/images_384_VarV2, gt_density_map_adaptive_384_VarV2, Train_Test_Val_FSC_147.json, annotation_FSC147_384.json
4. Checkpoint check: 4 expected .pth files under code/source-code/data/
5. .env check: code/source-code/.env with GEMINI_API_KEY

If any check still references /mnt/mmlab2024nas, fix to use local paths. Exit code must be 0 on all pass, 1 otherwise.
  </action>
  <verify>
    <automated>bash -n scripts/verify_env.sh &amp;&amp; ! grep -q "mmlab2024nas" scripts/verify_env.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <done>verify_env.sh passes bash -n; 5 check categories with PASS/FAIL; no NAS paths</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run setup on server and verify environment</name>
  <files>N/A — server-side execution</files>
  <action>Push scripts, SSH into sandbox.netbird.cloud, run server_setup.sh, upload data/checkpoints if missing, then verify_env.sh.</action>
  <verify>User reports all imports PASS and CUDA: True (Tesla T4)</verify>
  <done>Server env confirmed: .venv active, GPU accessible, local FSC-147 data and checkpoints present</done>
  <what-built>server_setup.sh and verify_env.sh updated for sandbox.netbird.cloud (venv, local paths, CUDA 13.2)</what-built>
  <how-to-verify>
Step 1 — Push scripts (local machine):
  git add scripts/ &amp;&amp; git commit -m "feat(01): update scripts for sandbox.netbird.cloud" &amp;&amp; git push

Step 2 — SSH into server:
  ssh sandbox.netbird.cloud

Step 3 — On server, run setup:
  bash scripts/server_setup.sh

Step 4 — Activate venv and verify:
  source ~/cs338-counting/.venv/bin/activate
  cd ~/cs338-counting
  bash scripts/verify_env.sh

Step 5 — If FSC-147 data is missing, upload from local machine:
  rsync -avz --progress code/source-code/data/FSC147/ sandbox.netbird.cloud:~/cs338-counting/code/source-code/data/FSC147/
  rsync -avz --progress code/source-code/data/*.pth sandbox.netbird.cloud:~/cs338-counting/code/source-code/data/

Expected outcome:
- All imports PASS
- CUDA device: Tesla T4
- All 4 FSC-147 local paths: OK
- At least 1/4 checkpoint files found
  </how-to-verify>
  <resume-signal>Paste the output of bash scripts/verify_env.sh or describe any FAIL items</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local to server (SSH key) | Code crosses network via key-based auth (id_ed20260404) |
| server to GitHub (HTTPS) | Repo clone over public HTTPS |
| server process to Gemini API | API key leaves server over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env with GEMINI_API_KEY on server | mitigate | .env in .gitignore; script prints reminder not to commit |
| T-01-02 | Information Disclosure | SSH private key id_ed20260404 on local | accept | User-managed; server has no password fallback |
| T-01-03 | Tampering | PyPI/GitHub packages during pip install | accept | Official PyTorch index + pinned ranges in requirements.txt |
| T-01-04 | Elevation of Privilege | venv pip install -e GroundingDINO | accept | Running as unprivileged phatcnguyen; no sudo required |
</threat_model>

<verification>
- bash -n scripts/server_setup.sh passes
- bash -n scripts/verify_env.sh passes
- Neither script references conda or /mnt/mmlab2024nas
- User reports: all imports PASS, CUDA: Tesla T4, local data paths verified
</verification>

<success_criteria>
- server_setup.sh and verify_env.sh committed for sandbox.netbird.cloud
- User confirms: .venv created, all imports work, Tesla T4 GPU accessible, FSC-147 data present
- Actual local data paths documented in SUMMARY.md
</success_criteria>

<output>
After completion, create .planning/phases/01-server-setup-baseline-reproduction/01-01-SUMMARY.md
</output>
