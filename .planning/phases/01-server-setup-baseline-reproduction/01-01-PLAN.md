---
phase: 01-server-setup-baseline-reproduction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/server_setup.sh
  - scripts/verify_env.sh
autonomous: false

requirements:
  - SRV-01
  - SRV-02
  - SRV-03

must_haves:
  truths:
    - "Server GPU is accessible (nvidia-smi shows a CUDA-capable device; torch.cuda.is_available() == True)"
    - "Conda env 'cs338' is created with all deps installed (torch, clip, groundingdino, timm, etc.)"
    - "FSC-147 images and annotation JSONs are present under /mnt/mmlab2024nas/counting/FSC147/"
    - "All 4 NSM checkpoints are present under /mnt/mmlab2024nas/counting/"
    - "GEMINI_API_KEY is configured in code/source-code/.env on server"
  artifacts:
    - path: "scripts/server_setup.sh"
      provides: "Reproducible conda-based environment setup script for counting@192.168.6.200"
      exports: []
    - path: "scripts/verify_env.sh"
      provides: "Quick smoke-test for all imports and NAS data paths"
      exports: []
  key_links:
    - from: "scripts/server_setup.sh"
      to: "code/source-code/GroundingDINO"
      via: "pip install -e ."
      pattern: "pip install -e"
    - from: "scripts/verify_env.sh"
      to: "code/source-code/data/FSC147"
      via: "path existence check"
      pattern: "test -e"
---

<objective>
Prepare server `counting@192.168.6.200` for reproducing the 4-config val-split evaluation.

Purpose: Establish a reproducible Conda environment on the lab server so all downstream experiment runs (Phases 2-4) execute consistently. Data lives on NAS at /mnt/mmlab2024nas/counting/.

Server specs: counting@192.168.6.200, Docker + Conda available, GPU TBD.
SSH: ssh counting@192.168.6.200 (password auth).

Output: scripts/server_setup.sh and scripts/verify_env.sh updated for counting@192.168.6.200 (Conda + NAS); user runs them on server and confirms environment works.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@scripts/setup_env.sh
@code/source-code/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify server_setup.sh for Conda env on counting@192.168.6.200</name>
  <files>scripts/server_setup.sh</files>
  <action>
Verify scripts/server_setup.sh has been updated for counting@192.168.6.200 (Conda, NAS data path).

It must contain all 8 steps:
1. Check NAS mount at /mnt/mmlab2024nas/counting
2. Clone/pull repo to ~/cs338-counting
3. Create Conda env 'cs338' (Python 3.10)
4. Install PyTorch via conda (pytorch-cuda=12.1)
5. Install GroundingDINO editable (pip install -e)
6. Install requirements.txt
7. Install CLIP via git+https
8. Check NAS data paths under /mnt/mmlab2024nas/counting/FSC147/ and checkpoints under /mnt/mmlab2024nas/counting/

If any step is missing, references old paths (sandbox.netbird.cloud / code/source-code/data / venv), fix it immediately.
  </action>
  <verify>
    <automated>bash -n scripts/server_setup.sh &amp;&amp; grep -q "mmlab2024nas" scripts/server_setup.sh &amp;&amp; grep -q "conda create\|CONDA_ENV" scripts/server_setup.sh &amp;&amp; ! grep -q "sandbox.netbird\|\.venv" scripts/server_setup.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <done>scripts/server_setup.sh passes bash -n; references mmlab2024nas + CONDA_ENV; no old venv/sandbox references</done>
</task>

<task type="auto">
  <name>Task 2: Verify verify_env.sh for local data paths</name>
  <files>scripts/verify_env.sh</files>
  <action>
Verify scripts/verify_env.sh has been updated for NAS data paths.

It must contain all 5 check categories:
1. Import checks: torch, clip, ultralytics, groundingdino, google.generativeai, timm — each as separate python -c call with PASS/FAIL output
2. GPU check: assert torch.cuda.is_available(), print device name
3. NAS data path checks: /mnt/mmlab2024nas/counting/FSC147/images_384_VarV2, gt_density_map_adaptive_384_VarV2, Train_Test_Val_FSC_147.json, annotation_FSC147_384.json
4. Checkpoint check: 4 expected .pth files under /mnt/mmlab2024nas/counting/
5. .env check: code/source-code/.env with GEMINI_API_KEY

DATA_BASE defaults to /mnt/mmlab2024nas/counting (overridable via env var). Exit code must be 0 on all pass, 1 otherwise.
  </action>
  <verify>
    <automated>bash -n scripts/verify_env.sh &amp;&amp; grep -q "mmlab2024nas" scripts/verify_env.sh &amp;&amp; grep -q "DATA_BASE" scripts/verify_env.sh &amp;&amp; echo "OK"</automated>
  </verify>
  <done>verify_env.sh passes bash -n; 5 check categories with PASS/FAIL; DATA_BASE defaults to /mnt/mmlab2024nas/counting</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Run setup on server and verify environment</name>
  <files>N/A — server-side execution</files>
  <action>Push scripts, SSH into counting@192.168.6.200 (pass: 1), run: conda activate cs338 && bash scripts/server_setup.sh; then upload FSC-147 data/checkpoints to /mnt/mmlab2024nas/counting/ if missing; then: bash scripts/verify_env.sh</action>
  <verify>User reports all imports PASS and CUDA: True</verify>
  <done>Server env confirmed: conda env cs338 active, GPU accessible, NAS FSC-147 data and checkpoints present at /mnt/mmlab2024nas/counting/</done>
  <what-built>server_setup.sh and verify_env.sh updated for counting@192.168.6.200 (Conda, NAS at /mnt/mmlab2024nas/counting)</what-built>
  <how-to-verify>
Step 1 — Push scripts (local machine):
  git add scripts/ &amp;&amp; git commit -m "feat(01): update scripts for counting@192.168.6.200" &amp;&amp; git push

Step 2 — SSH into server:
  ssh counting@192.168.6.200

Step 3 — On server, run setup:
  bash scripts/server_setup.sh

Step 4 — Activate venv and verify:
  conda activate cs338
  cd ~/cs338-counting
  bash scripts/verify_env.sh

Step 5 — If FSC-147 data is missing, upload from local machine:
  # Upload FSC-147 data to NAS (run from local machine):
  rsync -avz --progress code/source-code/data/FSC147/ counting@192.168.6.200:/mnt/mmlab2024nas/counting/FSC147/
  rsync -avz --progress code/source-code/data/*.pth counting@192.168.6.200:/mnt/mmlab2024nas/counting/

Expected outcome:
- All imports PASS
- CUDA device: <GPU name shown>
- All 4 FSC-147 local paths: OK
- At least 1/4 checkpoint files found
  </how-to-verify>
  <resume-signal>Paste the output of bash scripts/verify_env.sh or describe any FAIL items</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local to server (SSH key) | Code crosses network via key-based auth (id_ed20260404) |
| server to GitHub (HTTPS) | Repo clone over public HTTPS |
| server process to Gemini API | API key leaves server over HTTPS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | .env with GEMINI_API_KEY on server | mitigate | .env in .gitignore; script prints reminder not to commit |
| T-01-02 | Information Disclosure | SSH private key id_ed20260404 on local | accept | User-managed; server has no password fallback |
| T-01-03 | Tampering | PyPI/GitHub packages during pip install | accept | Official PyTorch index + pinned ranges in requirements.txt |
| T-01-04 | Elevation of Privilege | venv pip install -e GroundingDINO | accept | Running as unprivileged phatcnguyen; no sudo required |
</threat_model>

<verification>
- bash -n scripts/server_setup.sh passes
- bash -n scripts/verify_env.sh passes
- Neither script references conda or /mnt/mmlab2024nas
- User reports: all imports PASS, CUDA: True, NAS data paths verified at /mnt/mmlab2024nas/counting/
</verification>

<success_criteria>
- server_setup.sh and verify_env.sh committed for counting@192.168.6.200
- User confirms: conda env cs338 active, all imports work, GPU accessible, FSC-147 data present at /mnt/mmlab2024nas/counting/
- Actual local data paths documented in SUMMARY.md
</success_criteria>

<output>
After completion, create .planning/phases/01-server-setup-baseline-reproduction/01-01-SUMMARY.md
</output>
